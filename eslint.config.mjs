import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This rule fires on intentional, SSR-safe mount-time initialization
      // (reading sessionStorage / feature-detecting IntersectionObserver after
      // mount, first-visit fade-ins, etc.). Rewriting those to satisfy it would
      // risk hydration mismatches and the tuned animations, so keep it visible
      // as a warning rather than a build-breaking error.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
